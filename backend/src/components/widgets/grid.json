{
  "collectionName": "components_widgets_grids",
  "info": {
    "displayName": "grid",
    "icon": "dashboard",
    "description": ""
  },
  "options": {},
  "attributes": {
    "title": {
      "type": "string"
    },
    "desktopColumnCount": {
      "type": "enumeration",
      "enum": [
        "six",
        "four",
        "three",
        "two",
        "one"
      ],
      "default": "three"
    },
    "tabletColumnCount": {
      "type": "enumeration",
      "enum": [
        "six",
        "four",
        "three",
        "two",
        "one"
      ],
      "default": "two"
    },
    "mobileColumnCount": {
      "type": "enumeration",
      "enum": [
        "six",
        "four",
        "three",
        "two",
        "one"
      ],
      "default": "one"
    },
    "item": {
      "type": "component",
      "repeatable": true,
      "component": "components.grid-item"
    }
  }
}
